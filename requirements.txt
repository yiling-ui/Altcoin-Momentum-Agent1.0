# AMA-RT runtime dependencies.
#
# Phase 2 (Event Sourcing and Database) keeps the Phase 1 dependency set
# unchanged. We intentionally avoid exchange SDKs, LLM SDKs and Telegram
# libraries so that the safety foundation cannot accidentally connect to
# any real exchange, LLM or messaging service. Exchange SDK lands in
# Issue #3, Telegram outbound lands in Issue #10, LLM lands in Issue #10.

pydantic>=2.6,<3
pydantic-settings>=2.2,<3
PyYAML>=6.0
loguru>=0.7

# Dev / test
pytest>=8.0
pytest-asyncio>=0.23

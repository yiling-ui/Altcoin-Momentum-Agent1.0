# AMA-RT Phase 1 Safety Foundation runtime dependencies.
# Phase 1 intentionally avoids exchange SDKs, LLM SDKs and Telegram libraries
# to guarantee that the safety foundation cannot accidentally connect to any
# real exchange, LLM or messaging service.

pydantic>=2.6,<3
pydantic-settings>=2.2,<3
PyYAML>=6.0
loguru>=0.7

# Dev / test
pytest>=8.0
pytest-asyncio>=0.23

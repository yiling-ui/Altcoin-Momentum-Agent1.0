# AMA-RT secrets template.
#
# DO NOT put real credentials in this file. Copy to secrets.yaml (which is
# gitignored) and populate locally. Phase 1 does NOT consume any of these
# values; they exist only as a contract for future phases.

exchange:
  api_key: ""
  api_secret: ""

telegram:
  bot_token: ""
  admin_chat_ids: []

llm:
  deepseek_api_key: ""
